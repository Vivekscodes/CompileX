```javascript
try {
  // Attempt to use the native 'util.inherits' if available (Node.js environment)
  const util = require('util');

  // Check if util.inherits is a function to avoid issues in potentially incomplete environments.
  /* istanbul ignore next */
  if (typeof util.inherits !== 'function') {
    throw new Error('util.inherits is not a function'); // More descriptive error
  }

  // Export the native 'util.inherits' function
  module.exports = util.inherits;
} catch (e) {
  // If 'util' module or 'util.inherits' is not available (browser environment),
  // fallback to the browser-compatible implementation.
  /* istanbul ignore next */
  module.exports = require('./inherits_browser.js');
}
```